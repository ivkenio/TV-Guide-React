## TV GUIDE REACT
To setup the project, clone it in your projects directory, enter the project directory and run
         `$ npm install`
Then you need to setup your database connection. You will find it in root folder, then */server/config/config.json* file.
To run the project using `gulp` command.
